"""Platform layer: shared infrastructure adapters (database, cache).

Depends only on ``cyberai.core``. Contains no business rules - domain modules
consume these adapters, never the other way around.
"""
